# 📖 Quran Daily Tweet

بوت يقوم بنشر آية قرآنية عشوائية يومياً على منصة X (تويتر)، مع تغريدة خاصة للصلاة على النبي ﷺ كل يوم خميس.

---

## المميزات

- نشر آية قرآنية عشوائية مرتين يومياً
- تتبع الآيات المنشورة لتجنب التكرار
- تغريدة ثابتة يوم الخميس للصلاة على النبي ﷺ
- دعم وضع التجربة بدون نشر فعلي
- جدولة مرنة عبر متغيرات البيئة

---

## المتطلبات

- Node.js >= 18.0.0
- حساب مطور على منصة X (تويتر) مع صلاحيات القراءة والكتابة

---

## التثبيت

```bash
git clone https://github.com/yourusername/quran-daily-tweet.git
cd quran-daily-tweet
npm install
```

---

## الإعداد

انسخ ملف `.env.example` وأعد تسميته إلى `.env`:

```bash
cp .env.example .env
```

ثم أضف مفاتيح X API:

```env
X_API_KEY=your_api_key
X_API_SECRET=your_api_secret
X_ACCESS_TOKEN=your_access_token
X_ACCESS_TOKEN_SECRET=your_access_token_secret

# اختياري - الجدولة الافتراضية: مرتين يومياً (9ص و9م)
CRON_SCHEDULE=0 9,21 * * *
TIMEZONE=Asia/Muscat

# اختياري - تعطيل النشر الفعلي (لوضع التجربة)
ENABLE_POSTING=true
```

---

## التشغيل

```bash
# تشغيل البوت
npm start

# تشغيل مع إعادة التحميل التلقائي (للتطوير)
npm run dev
```

---

## السكريبتات

| الأمر                   | الوصف                         |
| ----------------------- | ----------------------------- |
| `npm run test:tweet`    | اختبار نشر تغريدة فعلية       |
| `npm run test:ayah`     | اختبار جلب آية وعرضها دون نشر |
| `npm run test:thursday` | اختبار تغريدة يوم الخميس      |
| `npm run stats`         | عرض إحصائيات الآيات المنشورة  |

---

## الجدولة

| المهمة        | الجدول         | الوقت                    |
| ------------- | -------------- | ------------------------ |
| آية يومية     | `0 9,21 * * *` | 9:00 صباحاً و 9:00 مساءً |
| تغريدة الخميس | `0 19 * * 4`   | 7:00 مساءً كل خميس       |

---

## البنية

```
src/
├── index.js              # نقطة الدخول الرئيسية
├── config/
│   └── index.js          # إعدادات ومتغيرات البيئة
├── scheduler/
│   └── index.js          # جدولة المهام
├── services/
│   ├── quran.js          # جلب وتنسيق الآيات
│   └── twitter.js        # التفاعل مع X API
├── scripts/
│   ├── test-tweet.js     # اختبار النشر
│   ├── test-ayah.js      # اختبار الآيات
│   ├── test-thursday.js  # اختبار تغريدة الخميس
│   └── show-stats.js     # عرض الإحصائيات
└── utils/
    └── logger.js         # نظام السجلات
```

---

## الترخيص

MIT
