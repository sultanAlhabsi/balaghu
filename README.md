# 🕌 Quran Daily Tweet

خدمة نشر آية قرآنية يومياً على منصة X (تويتر)

## 📝 الوصف

هذه الخدمة تقوم تلقائياً بـ:

- جلب آية قرآنية عشوائية يومياً من API القرآن
- تنسيق الآية بشكل جميل
- نشرها على منصة X في الوقت المحدد

### مثال على المنشور:

```
﴿ إِنَّ اللَّهَ مَعَ الصَّابِرِينَ ﴾

— سورة البقرة | آية 153

#قرآن #آية_اليوم
```

## 🚀 البدء السريع

### المتطلبات

- Node.js v18 أو أحدث
- حساب مطور على منصة X مع صلاحيات الكتابة

### التثبيت

1. **استنساخ المشروع:**

```bash
git clone <repository-url>
cd quran-daily-tweet
```

2. **تثبيت الحزم:**

```bash
npm install
```

3. **إعداد متغيرات البيئة:**

```bash
cp .env.example .env
```

4. **تعديل ملف `.env` بمفاتيح API:**

```env
X_API_KEY=your_api_key_here
X_API_SECRET=your_api_secret_here
X_ACCESS_TOKEN=your_access_token_here
X_ACCESS_TOKEN_SECRET=your_access_token_secret_here
```

5. **تشغيل الخدمة:**

```bash
npm start
```

## ⚙️ متغيرات البيئة

| المتغير                 | الوصف                     | مطلوب | القيمة الافتراضية |
| ----------------------- | ------------------------- | ----- | ----------------- |
| `X_API_KEY`             | مفتاح API من منصة X       | ✅    | -                 |
| `X_API_SECRET`          | المفتاح السري لـ API      | ✅    | -                 |
| `X_ACCESS_TOKEN`        | رمز الوصول                | ✅    | -                 |
| `X_ACCESS_TOKEN_SECRET` | المفتاح السري لرمز الوصول | ✅    | -                 |
| `CRON_SCHEDULE`         | جدولة النشر (تنسيق cron)  | ❌    | `0 9 * * *`       |
| `TIMEZONE`              | المنطقة الزمنية           | ❌    | `Asia/Muscat`     |
| `ENABLE_POSTING`        | تفعيل/تعطيل النشر         | ❌    | `true`            |
| `LOG_LEVEL`             | مستوى السجلات             | ❌    | `info`            |

## 📋 الأوامر

```bash
# تشغيل الخدمة
npm start

# التشغيل في وضع التطوير (مع إعادة التشغيل التلقائي)
npm run dev

# اختبار جلب آية (بدون نشر)
npm run test:ayah

# اختبار نشر آية فعلياً
npm run test:tweet
```

## 🏗️ هيكل المشروع

```
quran-daily-tweet/
├── src/
│   ├── config/
│   │   └── index.js          # إعدادات التطبيق
│   ├── services/
│   │   ├── quran.service.js  # خدمة API القرآن
│   │   ├── x.service.js      # خدمة API منصة X
│   │   └── tweet.service.js  # تنسيق ونشر التغريدات
│   ├── scheduler/
│   │   └── index.js          # مُجدوِل المهام
│   ├── utils/
│   │   └── logger.js         # أداة التسجيل
│   ├── scripts/
│   │   ├── test-ayah.js      # اختبار جلب الآيات
│   │   └── test-tweet.js     # اختبار النشر
│   └── index.js              # نقطة الدخول
├── .env.example              # مثال لمتغيرات البيئة
├── .gitignore
├── package.json
└── README.md
```

## 🔑 الحصول على مفاتيح API من منصة X

1. اذهب إلى [X Developer Portal](https://developer.twitter.com/)
2. أنشئ مشروعاً جديداً
3. أنشئ تطبيقاً داخل المشروع
4. في إعدادات التطبيق:
   - فعّل **OAuth 1.0a**
   - اختر صلاحيات **Read and Write**
5. انسخ:
   - API Key & Secret من قسم "Keys and tokens"
   - Access Token & Secret (قم بإنشائها إذا لم تكن موجودة)

## ☁️ النشر

### على سيرفر (PM2)

```bash
# تثبيت PM2
npm install -g pm2

# تشغيل الخدمة
pm2 start src/index.js --name "quran-daily-tweet"

# التشغيل عند إعادة تشغيل السيرفر
pm2 startup
pm2 save
```

### على Docker

```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
CMD ["npm", "start"]
```

```bash
docker build -t quran-daily-tweet .
docker run -d --env-file .env quran-daily-tweet
```

### على Railway / Render / Fly.io

1. اربط المستودع
2. أضف متغيرات البيئة في لوحة التحكم
3. سيتم النشر تلقائياً

## 📊 مستويات السجلات

- `debug` - تفاصيل كاملة للتتبع
- `info` - معلومات عامة (افتراضي)
- `warn` - تحذيرات
- `error` - أخطاء فقط

## 🔄 تخصيص وقت النشر

لتغيير وقت النشر، عدّل `CRON_SCHEDULE`:

```env
# كل يوم الساعة 9 صباحاً
CRON_SCHEDULE=0 9 * * *

# كل يوم الساعة 6 مساءً
CRON_SCHEDULE=0 18 * * *

# كل 12 ساعة
CRON_SCHEDULE=0 */12 * * *
```

## 📝 الترخيص

MIT License

## 🤝 المساهمة

المساهمات مرحب بها! افتح Issue أو Pull Request.

---

صُنع بـ ❤️ لنشر كلام الله
