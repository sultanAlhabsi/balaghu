version: '3.8'

services:
  quran-daily-tweet:
    build: .
    container_name: quran-daily-tweet
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
